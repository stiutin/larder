/** Public contact channels. Kept in one place so the Contact page and the footer never disagree. */
export const GITHUB_HANDLE = 'stiutin';
export const GITHUB_URL = `https://github.com/${GITHUB_HANDLE}`;
